# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
coverage
web-ext.config.ts
.factory
__pycache__

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Worktrees
.worktrees/

# Antigravity CLI
.antigravitycli/

# Workbuddy AI
.workbuddy-ai/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Reference / vendored extension code (not part of this repo)
ImmersiveTransalteExtensionCode/

# Beads / Dolt files (added by bd init)
.beads/proxieddb/

# Beads schema-migration gate flock names — never deleted by bd (the pattern
# below is the one `bd doctor --fix` installs). Runtime state, not tracked.
*.gate.lock*

# Smoke test files flagged by AV
**/tests/test_smoke.py
