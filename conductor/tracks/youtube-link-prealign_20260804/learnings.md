# Track Learnings: youtube-link-prealign_20260804

Patterns, gotchas, and context discovered during implementation.

## Codebase Patterns (Inherited)

📚 114 pattern sections in `conductor/patterns.md`; seeded from direct predecessor
`youtube-asr-resegment_20260709`.

### From youtube-asr-resegment_20260709 (direct predecessor)
- Pure libs under `lib/` with no Chrome APIs — preferred for TDD; a pure lib cannot
  network — AI calls must go through background/pool.
- Word-level only when JSON3 segs have explicit `tOffsetMs`; multi-seg without offsets =
  coarse join (not per-seg words).
- ASR detect = `kind=asr` URL OR `isAutoGenerated` track meta.
- Pipeline order: parse → resegment → translate → adaptCueTimings (display path).
- Prefer LLM returning index ranges (not free text/timestamps) so timing stays
  deterministic; normalize ranges to a full partition.
- Coordinator tests: `vi.resetModules()` before dynamic import; call `startCoordinator()`
  explicitly.

### Cache/progress spec invariants (docs/superpowers/specs/2026-07-27)
- Cache key `ai:{videoId}:{language}:{contentHash}`; contentHash over the raw ASR input
  units (text + timings), so caption body changes miss correctly.
- Language: track language code as used by the coordinator (`originalLanguage || 'en'`).
- Successful runs saved on device; changing models does not auto-invalidate (Force re-run
  is the manual escape hatch).

### Related archived tracks
- `youtube-asr-resegment_20260709` — the resegment engine + AI path this track pre-warms
- `subtitles-ux-refactor_20260706` — SubtitlesSection single-concern card patterns

---

<!-- Learnings from implementation will be appended below -->

## [2026-08-04 13:41] - Phase 1 Tasks 1-3: Pure watch-page extraction lib
- **Implemented:** `lib/youtubeWatchPage.ts` — `extractPlayerResponseFromWatchHtml` (balanced-brace scan skipping string contents; first parseable object wins), `selectAsrTrack` (typed `no-tracks`/`no-asr`), `buildJson3TimedtextUrl` (URL API + string-surgery fallback). 19 tests green.
- **Files changed:** `lib/youtubeWatchPage.ts`, `lib/__tests__/youtubeWatchPage.test.ts`
- **Commit:** phase-1 commit (see git log)
- **Learnings:**
  - Patterns: extractor returns `{ rawJson, data }` — `rawJson` feeds `YouTubeHandler.extractAvailableTracks(body)` directly (zero duplicated track parsing), `data.videoDetails` gives title for the cache entry.
  - Gotchas: a malformed first `ytInitialPlayerResponse` assignment can swallow later scripts into the brace scan — the whitespace-only guard between `=` and `{` plus JSON.parse gating keeps it safe; iterate all marker matches.
  - Context: ASR detect parity with playback gate = `isAutoGenerated` meta OR `isYoutubeAsrUrl(url)` (`kind=asr` param); tracks without `url` are unusable → `no-asr`.
---

## [2026-08-04 13:46] - Phase 2 Tasks 1-4: Background orchestration & permissions
- **Implemented:** `services/youtubeLinkPrealign.ts` (`runYoutubeLinkPrealign` with injected `fetchFn`/`resolveService`/broadcasts), `REALIGN_YOUTUBE_URL` + `RealignYoutubeUrlResult` (typed error codes) + `ASR_REALIGN_PROGRESS_BROADCAST` in types/messages.ts, `handleMessage` case in background.ts, `*://*.youtube.com/*` in static host_permissions (verified in built manifest).
- **Files changed:** services/youtubeLinkPrealign.ts, services/__tests__/youtubeLinkPrealign.test.ts, services/background.ts, types/messages.ts, wxt.config.ts
- **Commit:** phase-2 commit (see git log)
- **Learnings:**
  - Patterns: background orchestration DI seam — inject `fetchFn`/`resolveService`/broadcast callbacks so the module never imports `background.ts` (avoids an ESM cycle: background imports the handler) and tests need no chrome/global stubs beyond idb-keyval.
  - Gotchas: `ProviderPoolCoordinator.resegmentYoutubeAsr` catches `PoolExhaustedError` into `{success:false,error}` — map `/pool is empty|no providers configured/i` → `provider-not-configured` at the orchestration layer (UI must not parse English, so keep the mapping next to the call).
  - Context: single-flight key shape `${language}:${contentHash}` is shared with `handleResegmentYoutubeAsr` — a Settings run and a playback run of the same caption body dedupe each other. Timedtext baseUrl from the player response is untrusted → youtube-only host guard before fetch.
---

## [2026-08-04 13:52] - Phase 3 Tasks 1-3: fmt=json3 cache-key parity
- **Implemented:** proactive playback fetch canonicalized via `buildJson3TimedtextUrl(track.url)` in `activateYoutubeTrackViaPipelineInner`; hash-parity test pinning Settings-flow units == playback-flow units (and identical cache key) for the same json3 body.
- **Files changed:** content/subtitleCoordinator.ts, content/__tests__/subtitleCoordinator.test.ts, services/__tests__/youtubeLinkPrealign.test.ts
- **Commit:** phase-3 commit (see git log)
- **Learnings:**
  - Gotchas: canonicalize ONLY the fetch URL — track identity (`fetchedTrackUrls`, `activeTrackIdentity`) and the native-caption fallback (`YOUTUBE_REQUEST_CAPTIONS` → inject-side URL matching) must keep the original URL; one existing test asserted the exact pre-parity fetch URL and needed updating.
  - Context: proactive-path contentType detection already falls back to `body.startsWith('{')`, so json3 parses even when `track.url` lacks the fmt param; the parity fix only needed the fetch URL.
---

## [2026-08-04 13:57] - Phase 4 Tasks 1-3: Subtitle Studio UI card
- **Implemented:** `PrealignFromLinkCard.tsx` (URL validation, run states, batch progress i/n, already-saved, 7 typed errors, token-cost note, DisabledDimmer) wired between Caption quality and Saved caption re-aligns (stagger indices 2/3/4/5).
- **Files changed:** entrypoints/options/sections/subtitles/PrealignFromLinkCard.tsx, its test, SubtitlesSection.tsx, SubtitlesSection.test.tsx
- **Commit:** phase-4 commit (see git log)
- **Learnings:**
  - Patterns: options-page progress from background = runtime broadcast (`ASR_REALIGN_PROGRESS_BROADCAST` with `videoId` correlation) because options has no tab id; the tab-scoped `ASR_REALIGN_PROGRESS` stays for content scripts.
  - Gotchas: `findByTestId` resolves on the transient running status — assert final states with `waitFor` on text content; a static token-cost note can collide with status text in `getByText` — scope assertions to the status testid.
---

## [2026-08-04 14:04] - Phase 5 Tasks 1-2: Regression gates
- **Implemented:** full `vitest run` 633/633 green on the final tree; `eslint .` 0 errors; `tsc --noEmit` 0 errors; `wxt build` green.
- **Learnings:**
  - Gotchas: full-suite timing-flaky tests (5s timeout) under parallel load — `StatisticsSection` interactions, `AdvancedSection.backup` undo toast, `background.test.ts` chunk-retry. Failures move between runs and pass in isolation; verify with an isolation run + a clean full-suite re-run before claiming a regression.
---
